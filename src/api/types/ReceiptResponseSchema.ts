/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Monite from "../index.js";

export interface ReceiptResponseSchema {
    /** Unique receipt ID. */
    id: string;
    /** Creation timestamp. */
    created_at: string;
    /** Update timestamp. */
    updated_at: string;
    /** Entity user who created. */
    created_by_entity_user_id?: string;
    /** Currency code. */
    currency?: Monite.CurrencyEnum;
    /** Currency exchange details. */
    currency_exchange?: Record<string, unknown>;
    /** Receipt number. */
    document_id?: string;
    /** The id of the receipt file stored in the file saver. */
    file_id?: string;
    /** The URL of the receipt file stored in the file saver. */
    file_url?: string;
    /** Date when the receipt was issued. */
    issued_at?: string;
    /** Merchant location. */
    merchant_location?: string;
    /** Merchant name. */
    merchant_name?: string;
    /** OCR request id. */
    ocr_request_id?: string;
    /** OCR status. */
    ocr_status?: string;
    /** Partner metadata. */
    partner_metadata?: Record<string, unknown>;
    /** Source of data. */
    source_of_data?: Monite.SourceOfReceiptDataEnum;
    /** Total amount in minor units. */
    total_amount?: number;
    /** Transaction ID. */
    transaction_id?: string;
}
