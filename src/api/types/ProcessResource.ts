/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Monite from "../index.js";

export interface ProcessResource {
    id: string;
    created_at: string;
    updated_at?: string;
    created_by?: string;
    /** The error for the process. */
    error?: Record<string, unknown>;
    /** The input for the script. */
    input: Record<string, unknown>;
    /** The metadata for the process. */
    metadata: Record<string, unknown>;
    /** The script snapshot taken when script started. */
    script_snapshot?: ProcessResource.ScriptSnapshot;
    /** Tthe current status of the approval policy process. */
    status: Monite.ProcessStatusEnum;
    updated_by?: string;
}

export namespace ProcessResource {
    /**
     * The script snapshot taken when script started.
     */
    export type ScriptSnapshot = boolean | number | string | unknown[] | Record<string, unknown>;
}
